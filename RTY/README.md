# RTY Project Bundle

This folder contains the IARS full-stack project files, copied for easy upload to a new Git repository named `RTY`.

## Included
- `client/` frontend dashboard
- `server/` Express API with auth/chat
- This README

## Start server
```bash
cd server
npm install
npm run dev
```

Then open `client/index.html` in browser.
